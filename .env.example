# Copy to .env and fill in real values.

# Postgres (defaults to local SQLite if unset)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/oralverse

# Claude API (Phase 7 — report parsing)
ANTHROPIC_API_KEY=sk-ant-...

# Optional override for SAM 2 checkpoint location
# SAM2_CHECKPOINT=ai/segment/weights/sam2_hiera_small.pt

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
